/**
 * File:
 *   firewall_write.ycp
 *
 * Module:
 *   Configuration of firewall
 *
 * Summary:
 *   Writing only client
 *
 * Authors:
 *   Michael Hager <mike@suse.de>
 *
 * $Id$
 *
 * This is a write-only client. It takes its arguments and just
 * write the settings.
 */

/**
 * @param first a map of firewall settings
 * @return boolean success of operation
 * @example map mm = $[ "FAIL_DELAY" : "77" ];
 * @example any ret = WFM::CallModule("firewall_write",[mm]);
 */

{

textdomain "firewall";
include "firewall/io.ycp";

list args = WFM::Args();

if(size(args)<=0) {
    y2error("NOT writing, probably some mistake...");
    return false;
}

if(!is(WFM::Args(0),map)) {
    y2error("Bad argument for firewall write: %1", WFM::Args(0));
    return false;
}

map settings = select(args,0, $[]);
y2milestone("Only writing... (%1)",settings);

return FirewallWrite(settings);

} /* EOF */
